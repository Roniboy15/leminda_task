import React, { useContext } from "react"
import { Route } from "react-router-dom"

export const clientRoutes = () => {
  return (
    <React.Fragment>
      <Route path="/" element={} />
    </React.Fragment>
  )
}
